# 🤖 Affiliate Lead Bot — Инструкция по запуску

## Шаг 1 — Создай бота в Telegram

1. Напиши [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь команду `/newbot`
3. Придумай имя и юзернейм боту
4. Скопируй **токен** — он выглядит так: `7123456789:AAFxxx...`

---

## Шаг 2 — Узнай ID группы администраторов

1. Добавь бота [@getidsbot](https://t.me/getidsbot) в свою группу админов
2. Он пришлёт ID группы — он выглядит так: `-1001234567890`
3. Скопируй его

---

## Шаг 3 — Вставь токен и ID в bot.py

Открой файл `bot.py` и замени две строки вверху:

```python
BOT_TOKEN = "сюда вставь токен от BotFather"
ADMIN_CHAT_ID = "сюда вставь ID группы"
```

---

## Шаг 4 — Запусти бота (два варианта)

### Вариант A: Railway.app (бесплатно, без сервера)

1. Зайди на [railway.app](https://railway.app) и зарегистрируйся через GitHub
2. Нажми **New Project → Deploy from GitHub repo**
3. Загрузи файлы `bot.py` и `requirements.txt`
4. В разделе **Variables** добавь:
   - `BOT_TOKEN` = твой токен
   - `ADMIN_CHAT_ID` = ID группы
5. В `bot.py` замени строки токена на:
   ```python
   import os
   BOT_TOKEN = os.environ["BOT_TOKEN"]
   ADMIN_CHAT_ID = os.environ["ADMIN_CHAT_ID"]
   ```
6. Railway сам установит зависимости и запустит бота

### Вариант B: Локально на компьютере (для теста)

```bash
pip install -r requirements.txt
python bot.py
```

---

## Как добавить бота в группу админов

1. Открой группу → Добавить участника → найди своего бота по юзернейму
2. Дай ему права **отправлять сообщения**
3. Готово — заявки будут приходить в группу

---

## Что получат администраторы

Каждая заявка придёт в таком формате:

```
🔔 Новая заявка!

👤 Иван Иванов (@username)
🇷🇺 Язык: RU
🏷 Роль: Media Buying

💬 Запрос:
Интересует трафик на нутру, есть бюджет $5k/мес
```
